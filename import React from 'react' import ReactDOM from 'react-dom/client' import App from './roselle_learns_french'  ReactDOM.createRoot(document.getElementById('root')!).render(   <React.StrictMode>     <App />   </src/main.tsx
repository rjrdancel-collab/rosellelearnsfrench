import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './roselle_learns_french'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
